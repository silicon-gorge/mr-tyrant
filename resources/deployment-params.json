{
   "healthCheckType" : "EC2",
   "notificationDestination" : "contact.email@nokia.com",
   "selectedLoadBalancers" : "{{app-name}}",
   "selectedSecurityGroups" : [
      "{{web-security-group}}",
      "{{ssh-security-group}}"
   ]
}
